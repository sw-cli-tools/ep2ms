[package]
name = "ep2ms"
version = "0.1.0"
edition = "2024"
description = "Convert current time to milliseconds since Unix epoch"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = "0.4"

[build-dependencies]
hostname = "0.4"
chrono = "0.4"
